# starts the compose file c:
version: "3"
services:
  base-service:
    image: ipfs-tests
    cap_add:
      - NET_ADMIN
    networks: 
      - ipfs-network
    env_file: 
      - .ipfs-env
    volumes:
      - ${SHARED_VOLUME}:${EXP_SHARED_VOLUME}
    deploy:
      restart_policy:
        condition: none
      resources:
        limits:
          memory: 1G
